termios_p = struct define the mode of term
{
    tcflag_t c_iflag; input modes
    tcflag_t c_oflag; output modes
    tcflag_t c_cflag; control modes
    tcflag_t c_lflag; local modes
    cc_t c_cc[NCCS]; special characters
    speed_t c_ispeed;
    speed_t c_ospeed;
}

isatty(fd)
    test si le fd correspond a un term ouvert.
    EBADF == not valid fd
    EINVAL == file other than a terminal

ttyname(fd)
    Return terminal name, NULL if error

int ttyslot(void)
    Return index of the current term entry

int ioctl(int fd, unsigned long request, (char*||void*)macro)
    Use for manipulate underlying device parameters.
    Macro request located in sys/ioctl.

char* getenv(char *key)
    Return value of the key in env. Or NULL if error.q

tcsetattr(int fd, int opt_act, struct *termios term_p)
    Use the news term_p infos in actual term. And opt_act const in optional.
    opt_act:
        TCSANOW = Change the occurs immediatly
        TCSADRAIN = Change the occurs after all output writen
        to fd has been transmitted. Should be used if charging param modify output.
        TCSAFLUSH = Change occurs after all ouput writen to the object referd by fd been transmittedm and all input that has been received but not read.
tcgetattr
    Get term stuct, can be used for save entry config.
tgetent
•
tgetflag
•
tgetnum
•
tgetstr
•
tgoto
•
tputs
•
open
•
close
•
write
•
malloc
•
free
•
read
•
exit
•
signal